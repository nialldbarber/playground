import { colors } from "@/app/design-system/colors";
import { fontSize, lineHeight, radii, units } from "@/app/design-system/units";

export const theme = {
  colors,
  units,
  fontSize,
  lineHeight,
  radii,
} as const;
